import { cn } from '@/utils/cn';

export const progressStyles = cn(
  'relative h-4 w-full overflow-hidden rounded-full bg-secondary'
);

export const progressIndicatorStyles = cn(
  'h-full w-full flex-1 bg-primary transition-all'
);
