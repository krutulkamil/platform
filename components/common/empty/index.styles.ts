import { cn } from '@/utils/cn';

export const emptyWrapperStyles = cn(
  'h-full p-20 flex flex-col items-center justify-center'
);

export const imageWrapperStyles = cn('relative h-72 w-72');

export const labelWrapperStyles = cn(
  'text-muted-foreground text-sm text-center'
);
